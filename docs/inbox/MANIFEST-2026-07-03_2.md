# Inbox Manifest — 2026-07-03

| File | Destination | Action |
|---|---|---|
| CORRECTION-module-context-assembly-v1.0.md | docs/corrections/CORRECTION-module-context-assembly-v1.0.md | ADD |
| CORRECTION-feedback-plumbing-and-pipeline-fixes-v1.0.md | docs/corrections/CORRECTION-feedback-plumbing-and-pipeline-fixes-v1.0.md | ADD |

## Notes for Hermes

**Module context assembly (P1):** New subsystem — section-addressable module reads + per-prompt view map (`prompts/views.yaml`) + assembler (`src/context_assembly.py`). Removes all inline `[:2000]`-style module slices from pipeline routes in `src/app.py`. Also update CONTEXT.md with the "rules vs material" paragraph in §8 (copy verbatim). Prompt bumps: `assets/fan_out_v2.md` → v2.1 (adds `{visual_style}`).

**Feedback plumbing (F1/F2 = P1, F3 = P2, F4/F5 = P3):** F1 makes draft_text the editable authoritative artifact (new `/edit-text` endpoint, diff logged as weight-3 feedback, old direct_edit path returns 400). F2 feeds previous draft + weight-tagged feedback into regeneration. F3 differentiates series children via a breakdown prompt and spawns them in state `new`. F4 replaces the `draft_id + 100000` synthetic media ID with an `owner_type` column + migration. F5 ffprobes real durations in the edit-plan inventory.

**Coordination:** Both corrections touch `prompts/draft/generate_v2.md`. If implemented in one batch, land a single v2.2 containing the module-correction wording change AND the F2 revision block, and point `views.yaml` at v2.2. Suggested overall order: module-context-assembly first (F2 reuses its boundary-truncation helper), then F1 → F2 → F5 → F4 → F3.

Update the changelog and move this manifest to docs/inbox/processed/ when done.
