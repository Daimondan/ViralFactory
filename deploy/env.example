# /etc/viralfactory/env
# Create this file on the VPS (NOT committed to repo — holds secrets):
#   sudo mkdir -p /etc/viralfactory
#   sudo tee /etc/viralfactory/env << 'EOF'
#   OLLAMA_API_KEY=your_api_key_here
#   EOF
OLLAMA_API_KEY=your_api_key_here