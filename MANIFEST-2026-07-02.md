# MANIFEST — 2026-07-02 batch (architect → Hermes)

*Hermes: read this file completely before touching anything else in this batch. This is the first batch under the Inbox Protocol — see BOOTSTRAP below.*

## BOOTSTRAP (one-time)

This batch arrives at the **repo root** because `docs/inbox/` does not exist yet. Create it now:

1. Move `INBOX-README.md` → `docs/inbox/README.md` (this establishes the protocol — read it, its rules bind you from now on).
2. Create `docs/inbox/processed/` (a `.gitkeep` is fine).
3. All future batches arrive in `docs/inbox/`; the operator will say "check the inbox."

## FILES IN THIS BATCH

| File (as uploaded) | Action | Destination |
|---|---|---|
| `INBOX-README.md` | ADD | `docs/inbox/README.md` |
| `AMENDMENT-003-staged-content-pipeline.md` | ADD | `docs/decisions/AMENDMENT-003-staged-content-pipeline.md` |
| `diagrams-README.md` | REPLACE | `docs/diagrams/README.md` |
| `system-overview-v3.2.svg` | ADD | `docs/diagrams/system-overview-v3.2.svg` |
| `MANIFEST-2026-07-02.md` (this file) | MOVE after processing | `docs/inbox/processed/MANIFEST-2026-07-02.md` |
| `docs/stackpenni_v3_system_with_onboarding.png` (already in repo) | SUPERSEDE | leave in place — the replaced `docs/diagrams/README.md` already carries the supersession note; do not delete the PNG |

## APPLY (after filing, before returning to M2 work)

1. **Charter → v3.2:** replace the "The core loop" section of `docs/CHARTER-v3.1.md` with the replacement text in AMENDMENT-003, rename/version the file per your existing convention, and update all references (README, CONTEXT.md reading order, BUILD_PLAN header) to point at v3.2.
2. **CONTEXT.md:** mirror the staged loop (Ideas gate → Draft with visual direction, no renders → Assets per-platform gate → Publish go/hold; Gather configured by onboarding; ideas grounded in modules). CONTEXT conforms to charter — conflicts are bugs.
3. **UI-DIRECTION.md:** per AMENDMENT-003's diagram/docs section — Surface 2 gains the Ideas queue view (cards with origin badge, hook options, evidence links, approve/kill/park) and the Assets per-platform review view.
4. **BUILD_PLAN:** add the M3 tasks listed in AMENDMENT-003's BUILD_PLAN impact section (idea cards + Gate 1 UI, visual-direction block in draft schema, Assets stage + Gate 3 UI, `origin` field threaded through pipeline tables and nightly note). M2 is unchanged — continue it after this filing.
5. **Diagram instruction inside AMENDMENT-003 is ALREADY SATISFIED** by the files in this batch — only verify the Mermaid in the new `docs/diagrams/README.md` renders on GitHub.
6. **CHANGELOG:** one entry for the batch (filed files + charter bump to v3.2 + inbox protocol established).

## NOT in scope

No code changes. No M2 scope changes. Review-w1 corrections R1–R5 are confirmed applied and accepted by the architect (verified in code at commit d24c0c5); R6–R9 remain queued for M2 as written in `docs/review-w1_1.md`.
