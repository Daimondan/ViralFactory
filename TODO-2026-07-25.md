# TODO — 2026-07-25 Session Remaining Work

> Filed from operator session 2026-07-25. See `docs/inbox/BUILDER-NOTE-018-remaining-work-2026-07-25.md` for full details.

## P0 — Blocking
- [ ] **Fix render service not updating asset_state after successful render** — `src/services/render_review.py` needs `store.update_asset_state(asset_id, "rendered")` after successful render. Every future render gets stuck without this.

## P1 — Important
- [ ] **Add replacement RSS feeds to sources.yaml** — `feeds: []` needs real feeds for AI/wealth/Caribbean entrepreneurship. Idea diversity will stagnate without fresh source material.
- [ ] **Test approve → publish end-to-end** — 10 assets at Gate 3. Operator approves → Gate 4 → Buffer → verify publish_log. First ever publish test. Check Buffer credentials in models.yaml first.

## P2 — Enhancements
- [ ] **Design ending card templates** — title_card_v1 + quote_card_v1 for cinematic style. PIL overlay templates matching golden-hour tone, StackPenni palette, Montserrat Bold + Anton. Need operator input on card text (brand, handle, CTA).
- [ ] **Fix kill reason text not captured** — 18 feedback_log entries with empty feedback_text. Trace Gate 1 kill path in app.py, ensure kill reason text passes to store.add_feedback().