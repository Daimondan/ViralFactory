# prompts/views.yaml — module context view map (v1.0)
# section: one ## block   entry: one ### under a parent   index: generated entry list
# full: entire module     Budgets in chars, per projection, boundary-enforced by design.
#
# Renaming or removing a ## heading in a *_to_markdown generator is a breaking
# change — update this view map in the same commit.

ideas/concepts_v1.md:
  voice_profile:      {module: voice-profile,    mode: section, heading: "Identity line", budget: 2000,
                       fallback: full}
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 6000}
  audience_insights:  {module: audience-insights, mode: full, budget: 4000}
  story_frameworks:   {module: story-frameworks, mode: full,  budget: 4000}

ideas/treatment_select_v1.md:
  format_guide:       {module: format-guide, mode: section, heading: "Selection profiles", budget: 12000,
                       fallback: index}

ideas/generate_v1.md:
  voice_profile:      {module: voice-profile,    mode: section, heading: "Identity line", budget: 2000,
                       fallback: full}
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 6000}
  audience_insights:  {module: audience-insights, mode: full, budget: 4000}
  story_frameworks:   {module: story-frameworks, mode: full,  budget: 4000}
  format_guide:       {module: format-guide,     mode: section, heading: "Selection profiles", budget: 12000,
                       fallback: index}

draft/generate_v3.md:
  voice_profile:      {module: voice-profile,    mode: full,  budget: 8000}
  tells_checklist:    {module: voice-profile,    mode: section, heading: Tells Checklist, budget: 2000}
  ai_tells:           {file: shared/ai_tells_v1.md, budget: 8000}
  story_frameworks:   {module: story-frameworks, mode: full,  budget: 5000}
  audience_insights:  {module: audience-insights, mode: full, budget: 4000}
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 5000}
  visual_style:       {module: visual-style,     mode: full,  budget: 5000}
  format_guide:       {module: format-guide,     mode: entry, parent: Formats,
                       key: "{treatment.format_name}", budget: 4000,
                       fallback: index}

draft/generate_v2.md:
  voice_profile:      {module: voice-profile,    mode: full,  budget: 8000}
  tells_checklist:    {module: voice-profile,    mode: section, heading: Tells Checklist, budget: 2000}
  story_frameworks:   {module: story-frameworks, mode: full,  budget: 5000}
  audience_insights:  {module: audience-insights, mode: full, budget: 4000}
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 5000}
  visual_style:       {module: visual-style,     mode: full,  budget: 5000}
  format_guide:       {module: format-guide,     mode: entry, parent: Formats,
                       key: "{treatment.format_name}", budget: 4000,
                       fallback: index}

assets/fan_out_v2.md:
  visual_style:       {module: visual-style,     mode: section, heading: Platform adjustments, budget: 2000,
                       fallback: full}

assembly/edit_plan_v1.md:
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 3000}
  format_guide:       {module: format-guide,     mode: entry, parent: Formats,
                       key: "{format_name}", budget: 3000, fallback: index}
  visual_style:       {module: visual-style,     mode: full,  budget: 4000}

assembly/edit_plan_v2.md:
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 3000}
  format_guide:       {module: format-guide,     mode: entry, parent: Formats,
                       key: "{format_name}", budget: 3000, fallback: index}
  visual_style:       {module: visual-style,     mode: full,  budget: 4000}

assembly/media_plan_v1.md:
  visual_style:       {module: visual-style,     mode: full,  budget: 5000}

# T11.5: Episode format module — the show bible. Available to Writer and
# Assembler prompts. Full module: cast, world, grade, beat grammar, delivery
# mode, audio registers, graphics vocabulary, critic rubric.
draft/generate_v4.md:
  voice_profile:      {module: voice-profile,    mode: full,  budget: 8000}
  tells_checklist:    {module: voice-profile,    mode: section, heading: Tells Checklist, budget: 2000}
  ai_tells:           {file: shared/ai_tells_v1.md, budget: 8000}
  story_frameworks:   {module: story-frameworks, mode: full,  budget: 5000}
  audience_insights:  {module: audience-insights, mode: full, budget: 4000}
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 5000}
  visual_style:       {module: visual-style,     mode: full,  budget: 5000}
  format_guide:       {module: format-guide,     mode: entry, parent: Formats,
                       key: "{treatment.format_name}", budget: 4000,
                       fallback: index}
  episode_format:     {module: episode-format-parable, mode: full, budget: 8000,
                       fallback: full}

assembly/media_plan_v2.md:
  visual_style:       {module: visual-style,     mode: full,  budget: 5000}
  episode_format:     {module: episode-format-parable, mode: full, budget: 8000,
                       fallback: full}

assembly/edit_plan_v2.md:
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 3000}
  format_guide:       {module: format-guide,     mode: entry, parent: Formats,
                       key: "{format_name}", budget: 3000, fallback: index}
  visual_style:       {module: visual-style,     mode: full,  budget: 4000}
  episode_format:     {module: episode-format-parable, mode: full, budget: 8000,
                       fallback: full}

# F3: series breakdown — added by CORRECTION-feedback-plumbing-and-pipeline-fixes
ideas/series_breakdown_v1.md:
  viral_patterns:     {module: viral-patterns,   mode: full,  budget: 4000}
  audience_insights:  {module: audience-insights, mode: full, budget: 3000}
  story_frameworks:   {module: story-frameworks, mode: full,  budget: 3000}
  format_guide:       {module: format-guide,     mode: index, parent: Formats, budget: 2000}