# Core runtime — the app cannot start without these.
# Versions pinned from the live VPS venv (pip freeze) on 2026-07-25.
Flask==3.1.3
gunicorn==26.0.0
PyYAML==6.0.3
requests==2.34.2
Pillow==12.3.0
numpy==1.26.4